#include <stdio.h>

/**
 * main - Prints all possible combinations of three different digits,
 *        in ascending order, separated by a comma followed by a space.
 *
 * Return: Always 0.
 */
int main(void)
{
fprint(stdout, "%s", "and that piece of art is useful\"");
fprint(stdout, "%s", " - Dora korpar, 2015-10-19\n");

return (1);
}
